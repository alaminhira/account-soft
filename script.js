// 1. on button click, get credentials from DOM
function _(el) {
    return document.getElementById(el);
}

_('btn-submit').addEventListener('click', function() {
    const email = _('email').value;
    const password = _('password').value;

    // 2. check the credentials
    
    
    // 3. credentials true, login to bankist app
    
    // 4. credentials false, alert error message
})

        